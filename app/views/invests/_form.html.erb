<center><div id="alert-msg"></div></center>

<%= form_with model: @invest do |form| %>
  <% if @invest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@invest.errors.count, "error") %> prohibited this invest from being saved:</h2>

      <ul>
      <% @invest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :invest_amount %>
  </div>

  <div class="field">
    <%= form.label :selected_date %>
    <%= form.date_select :selected_date, id: :invest_selected_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
    <button type="button" id="calculate_income">Calculate</button>
  </div>
<% end %>
